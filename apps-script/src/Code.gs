/**
 * INTENTIONALLY EMPTY.
 *
 * The Accounting Manual master spreadsheet must NOT contain any logic. All behavior is now
 * delivered to client copies by the internal Editor Add-on (apps-script-addon) which attaches
 * a small bound shim that delegates to the AccountingLib library (apps-script-lib).
 *
 * Do not put functions here. If you do, they will end up in client copies that are made via
 * Drive copy and you will fight against the add-on / library architecture.
 */
